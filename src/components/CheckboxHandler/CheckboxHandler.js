import React from 'react'
import { Container } from './CheckboxHandler.style'
function CheckboxHandler({ children }) {
  return (
    <Container>{children}</Container>
  )
}

export default CheckboxHandler